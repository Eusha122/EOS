# EOS Privet fresh sessions are intentionally password-free. The user is
# created by live-config from the manifest-defined boot parameter and the
# encrypted-persistence unlock flow is handled before this display manager.
[Autologin]
User=@EOS_LIVE_USER@
Session=plasma.desktop
Relogin=false
