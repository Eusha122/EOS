# EOS Desktop App

This package boundary is reserved for the future EOS website application that will ship by default with the operating system.

When the website is ready, package it here (for example as a Tauri, Electron, or native web-app package) and keep its integration limited to this component. The live-build configuration should only install or include the published package.

Current state: placeholder launcher only; no website source has been added.
